# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Database
# For optimal performance with Supabase, use the connection pooling URL (port 6543 with pgBouncer)
# Connection pooling parameters for serverless environments:
# - pgbouncer=true: Enable pgBouncer mode
# - connection_limit=10: Max connections per Prisma Client instance (5-10 for serverless)
# - pool_timeout=10: Max seconds to wait for a connection from the pool
# - connect_timeout=10: Max seconds to establish initial database connection
DATABASE_URL=postgresql://postgres:[password]@[host]:6543/[database]?pgbouncer=true&connection_limit=10&pool_timeout=10&connect_timeout=10

# Claude AI
ANTHROPIC_API_KEY=your-anthropic-api-key

# Cron Secret (for vercel cron authentication)
CRON_SECRET=your-random-secret-string

# CSRF Protection (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
CSRF_SECRET=your-32-byte-random-hex-string

# Email (optional - for sending notifications)
RESEND_API_KEY=your-resend-api-key
FROM_EMAIL=noreply@yourdomain.com
